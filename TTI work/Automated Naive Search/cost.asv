function final_cost=cost(test, contact, I,space)
%spacing is determeind by ODE, how small are time steps
%test, contact, and I are vectors describing how mnay people were tested
%each time step, how many people were traced each time step,,
k_1=50;
k_2=100;
final_cost = trap(space,k_1 *test) + trap(space,k_2*contact)+ trap(space,hospital_capacity(I));

end